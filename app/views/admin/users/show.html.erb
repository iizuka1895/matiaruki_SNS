<%= @user.name %>さんの会員詳細
<table>
  <tr>
    <th>会員ID</th>
    <td><%= @user.id %></td>
  </tr>
  <tr>
    <th>ハンドルネーム</th>
    <td><%= @user.name %>
    </td>
  </tr>
  <tr>
    <th>会員ステータス</th>
    <% if @user.is_active == false %>
      <td class="text-secondary">無効</td>
    <% else %>
      <td class="text-success">有効</td>
    <% end %>
  </tr>
</table>
<div>
  <p><%= link_to "編集する", edit_admin_user_path %></p>
</div>