
  <div>
    <%= link_to notification.subject.user.name, public_user_path(notification.subject.user) %>
    があなたの
    <%= link_to '投稿', public_user_post_path(notification.subject.user, notification.subject.post) %>
    にいいねしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
