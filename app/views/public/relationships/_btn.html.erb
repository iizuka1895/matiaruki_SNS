<% if current_user != user.id %>
  <% if current_user.following?(user) %>
    <%= button_to "フォローを外す", public_user_relationships_path(user.id), method: :delete %>
  <% else %>
    <%= button_to "フォローする", public_user_relationships_path(user.id), method: :post %>
  <% end %>
<% end %>